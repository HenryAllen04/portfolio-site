import { essayMetadata } from "@/lib/writings";

export const metadata = essayMetadata("the20hr-co");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
